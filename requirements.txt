transformers>=4.41.0
torch>=2.2.0
datasets>=2.19.0
pyyaml>=6.0.0
numpy>=1.26.0
tqdm>=4.66.0
pydantic>=2.7.0
# Required for 4-bit quantized checkpoint loading on CUDA
bitsandbytes>=0.43.0
accelerate>=0.30.0

# Required to load the Llama-2 SentencePiece tokenizer used by the official
# HarmBench judge classifier (cais/HarmBench-Llama-2-13b-cls). The target
# models (Qwen3, Llama-3.2) ship a fast tokenizer.json and do not need these,
# but the older Llama-2 classifier does — two TC1 judge jobs failed without
# them before they were added (see PROJECT_LOG D16 / TC1 runbook).
sentencepiece>=0.2.0
protobuf>=4.0.0
tiktoken>=0.7.0

# Optional: second-judge cross-check via an OpenAI-compatible API (T22, W3).
# Only needed for `run_judge_validation.py --backend api_judge`; the primary
# HarmBench scorer (harmbench_cls) and the rest of the pipeline do not need it.
openai>=1.0.0

# Development / testing
pytest>=8.0.0
pytest-cov>=5.0.0
