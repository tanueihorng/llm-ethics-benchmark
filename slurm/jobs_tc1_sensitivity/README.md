# T18 sensitivity-arm sbatch files (generated)

Generated by `scripts/generate_sensitivity_jobs.py` — do not hand-edit; regenerate instead. Seeds: 1, 2, 3, 4, 5.

Run order on TC1 (one GPU job at a time):
1. Generation, load-bearing pair first: `sbatch slurm/jobs_tc1_sensitivity/qwen_2b_base__sens.sbatch` then `qwen_2b_4bit__sens.sbatch` (optionally the qwen_4b and llama pairs for full symmetry).
2. After generation for a seed completes, judge it: `sbatch slurm/jobs_tc1_sensitivity/judge_sens_seed1.sbatch` (repeat per seed; edit `--models` if you only generated one pair).
3. SCP `results_sensitivity/` back to the Mac and run `python scripts/sensitivity_analysis.py`.
