"""Streamlit dashboard for the fyp_quant quantization study.

``dashboard.data`` is the pure (Streamlit-free) data layer; ``dashboard.app`` is
the Streamlit UI. Launch with ``make dashboard`` or
``streamlit run dashboard/app.py``.
"""
