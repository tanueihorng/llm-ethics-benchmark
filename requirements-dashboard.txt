# Optional dependencies for the interactive dashboard (dashboard/app.py).
# Kept separate from requirements.txt so the core experiment environment stays
# minimal. Install with:  pip install -r requirements-dashboard.txt
streamlit>=1.30,<2
plotly>=5.18,<6
