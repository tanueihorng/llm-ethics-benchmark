# TC1 Work Log — 2026-04-08

This file records the setup, code changes, and TC1 onboarding work completed on 2026-04-08.

## Summary

- Reviewed the TC1 approval form, TC1 user guide, and the sample SLURM job script.
- Confirmed the approved TC1 account details:
  - Username: `utan001`
  - Home directory: `/tc1home/FYP/utan001`
  - Quota: `300 GB`
  - QoS: `normal`
  - Partition: `UGGPU-TC1`
  - Usage window: `03/2026` to `11/2026`
- Confirmed the effective QoS limits shown by `MyTCinfo`:
  - `cpu=20`
  - `gres/gpu=1`
  - `mem=64G`
  - `MaxSubmit=2`
  - `MaxWall=06:00:00`
- Completed the first SSH login to TC1 so the home directory was created and activated.
- Verified the home directory exists at `/tc1home/FYP/utan001`.
- Created the remote workspace folder `/tc1home/FYP/utan001/fyp_quant`.
- Inspected the remote home directory and confirmed it was still essentially fresh.
- Renamed the local project folder to `/Users/tanueihorng/fyp_quant`.

## Local Repository Work

- Added TC1-oriented cluster config at `configs/tc1.yaml`.
- Added support for SLURM jobs to:
  - `cd` into a configured working directory
  - run environment bootstrap commands before the Python entrypoint
- Updated the TC1 runbook so the generated job workflow matches TC1 usage.
- Generated TC1 job scripts under `slurm/jobs_tc1`.
- Verified the generated jobs include:
  - `#SBATCH --partition=UGGPU-TC1`
  - `#SBATCH --qos=normal`
  - `#SBATCH --gres=gpu:1`
  - `module load slurm`
  - `module load anaconda`
  - `source activate fyp-tc1`

## Stability and Testability Work

- Refactored evaluator imports to avoid loading heavy ML dependencies at import time.
- Refactored model imports to avoid eager `torch` and `transformers` imports.
- Updated tests to cover the lazy-loading behavior and the SLURM helper behavior.
- Verified the full local test suite passes:
  - `pytest -q`
  - Result: `113 passed`

## Remote TC1 Setup Completed Today

- Built a trimmed local archive of the repo at `/tmp/tc1_repo_trimmed_2026-04-08.tar.gz`.
- Uploaded that archive to `/tc1home/FYP/utan001/fyp_quant/` with `scp`.
- Opened a live SSH shell to TC1 and unpacked the archive into:
  - `/tc1home/FYP/utan001/fyp_quant/repo`
- Confirmed the repo contents are present on TC1.
- Removed macOS archive metadata files matching `._*` from the remote repo.
- Loaded the `anaconda` module on TC1.
- Observed that `conda create` initially failed because the Anaconda channel Terms of Service had not been accepted yet.
- Accepted the required channels:
  - `https://repo.anaconda.com/pkgs/main`
  - `https://repo.anaconda.com/pkgs/r`
- Successfully created the TC1 conda environment:
  - `fyp-tc1`
- Activated `fyp-tc1` successfully.
- Verified the environment Python version:
  - `Python 3.11.15`
- Installed the repository requirements into `fyp-tc1`.
- Verified the main ML stack imports cleanly on TC1:
  - `torch==2.11.0+cu130`
  - `transformers==5.5.0`

## Current Status

- TC1 environment setup is complete.
- The repo is present in `/tc1home/FYP/utan001/fyp_quant/repo`.
- The next step is no longer setup.
- The next step is to run a small controlled benchmark smoke test, then submit real SLURM jobs.

## Recommended Next Actions

- Generate fresh TC1 job scripts on TC1 using `configs/tc1.yaml`.
- Start with a very small Qwen-only smoke run to validate the execution path.
- After that succeeds, submit a first real single-model job.
- Only then scale to the broader matrix.
- Treat the Llama jobs as optional until Hugging Face access permissions are confirmed on TC1.

## Notes

- This log intentionally excludes passwords and other secrets.
- Heavy inference should run through SLURM jobs on TC1 compute nodes, not on the login node.
